import { homeNavStyle } from "./HomeStyles";

const HomeHeader = () => {
  return (
    <nav className={homeNavStyle}>
      <h1 className="text-white text-[40px]">Leads</h1>
    </nav>
  );
};

export default HomeHeader;